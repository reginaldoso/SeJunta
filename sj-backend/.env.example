# Copy to .env and set secure values for production
JWT_SECRET=change-this-to-a-secret
ENC_KEY=aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
DATABASE_URL=postgres://sj:sjpass@localhost:5432/sjdb
APP_BASE_URL=http://localhost:3000
# SMTP (optional) — if not provided, verification link will be logged
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=no-reply@sejunta.local
GOOGLE_MAPS_API_KEY=
